# Python dependencies for the decomposition-ensemble experiments.
#
# NOTE FOR THE AUTHORS: the packages below are those implied by the methods
# reported in the manuscript (Figure 1). Before submission, replace the
# ">=" constraints with the exact versions used to produce the results, e.g.
# by running `pip freeze > requirements.txt` inside the analysis environment.
# PeerJ expects the computational environment to be reproducible.

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# STL and robust STL decomposition, ARIMA and Diebold-Mariano / MCS testing
statsmodels>=0.14
pmdarima>=2.0

# Deep component learners (TCN, N-BEATS, GRU)
torch>=2.0
pytorch-lightning>=2.0
darts>=0.27

# Figures
matplotlib>=3.7
