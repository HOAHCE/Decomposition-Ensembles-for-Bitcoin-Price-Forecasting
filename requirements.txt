# Python dependencies for the decomposition-ensemble experiments.
#
# The experiments were run on Google Colab with a T4 GPU. TensorFlow 2.20.0 is
# the version the committed notebook recorded in its own output; the remaining
# packages were supplied by the Colab image or installed by the notebook's first
# cell, so their exact versions were not captured. Pin them to the versions in
# the run environment before submission (`pip freeze`).

# Deep component learners: TCN, N-BEATS, GRU (tf.keras)
tensorflow==2.20.0

# STL / robust STL decomposition and ARIMA
statsmodels>=0.14
pmdarima>=2.0

# Model Confidence Set and bootstrap inference
arch>=6.0

numpy>=1.26
pandas>=2.0
scipy>=1.11
matplotlib>=3.7

# Only needed by code/extract_notebook_results.py (parses the notebook's
# rendered HTML tables); not required to run the experiments.
lxml>=4.9
